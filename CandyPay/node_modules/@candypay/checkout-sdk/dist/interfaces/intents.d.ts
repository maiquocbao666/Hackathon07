import { CheckoutItemEntity, SupportedSplTokens } from '../types/index.js';
import { SessionMetadataResponse } from './sessions.js';

interface CreateIntentOptions {
    items: CheckoutItemEntity[];
    tokens?: SupportedSplTokens[];
    metadata?: {
        [k: string]: unknown;
    };
}
interface CreateIntentResponse {
    intent_id: string;
    order_id: string;
    intent_secret_key: string;
    metadata: Object;
    user: Object;
}
interface IntentMetadataOptions {
    intent_id: string;
}
interface IntentMetadataResponse extends SessionMetadataResponse {
    is_intent: boolean;
}

export { CreateIntentOptions, CreateIntentResponse, IntentMetadataOptions, IntentMetadataResponse };
